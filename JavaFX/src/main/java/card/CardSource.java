package card;

import javafx.scene.image.ImageView;

public class CardSource extends SpellCards{
    public CardSource(ImageView imageCard, String nameCard) {
        super(imageCard, nameCard);
    }
}
