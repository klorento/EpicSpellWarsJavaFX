package card;

import javafx.scene.image.ImageView;

public class CardDelivery extends SpellCards {
    public CardDelivery(ImageView imageCard, String nameCard) {
        super(imageCard, nameCard);
    }
}
